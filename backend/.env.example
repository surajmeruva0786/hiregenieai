# Node Server Configuration
NODE_ENV=development
PORT=5000

# PostgreSQL Configuration
# Option 1: Local PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=hiregenie
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Option 2: Supabase (Free Cloud PostgreSQL)
# Get from: https://supabase.com
# POSTGRES_HOST=db.xxx.supabase.co
# POSTGRES_PORT=5432
# POSTGRES_DB=postgres
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_supabase_password

# MongoDB Configuration
# Option 1: Local MongoDB
MONGODB_URI=mongodb://localhost:27017/hiregenie

# Option 2: MongoDB Atlas (Free Cloud MongoDB)
# Get from: https://www.mongodb.com/cloud/atlas
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/hiregenie

# Redis Configuration (Optional for basic testing)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Secrets (CHANGE THESE!)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change-this-to-a-random-64-character-string-min-32-chars
JWT_REFRESH_SECRET=change-this-to-another-random-64-character-string-min-32-chars

# Gemini AI API Key (Required for AI features)
# Get free key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Zoom API Configuration (Optional - for Zoom meeting integration)
# Get from: https://marketplace.zoom.us/
ZOOM_API_KEY=your_zoom_api_key_here
ZOOM_API_SECRET=your_zoom_api_secret_here

# Google Speech-to-Text (Optional - for voice transcription)
GOOGLE_SPEECH_API_KEY=your_google_speech_key_here

# WebSocket Configuration
WEBSOCKET_PORT=3001
FRONTEND_URL=http://localhost:5173

# SendGrid Email (Optional - for notifications)
# Get from: https://sendgrid.com
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# Stripe Payment (Optional - for billing)
# Get from: https://stripe.com
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_STARTER_PRICE_ID=
STRIPE_PROFESSIONAL_PRICE_ID=
STRIPE_ENTERPRISE_PRICE_ID=

# File Upload Configuration
MAX_FILE_SIZE=10485760

# QUICK START GUIDE:
# 1. Install PostgreSQL and MongoDB (or use cloud versions)
# 2. Set GEMINI_API_KEY (required)
# 3. Update database credentials above
# 4. Run: npm run dev
# 5. Test: http://localhost:5000/health
